Conexiones git

git init

git config --global user.name "nombre_usuario"

git config --global user.email "email_usuario"

git status

git add .(El punto es para todos los archivos o sino escribir nombre del archivo)

git commit -m "Comentario sobre los archivos para subir"

got remote add origin url_del_git (UNA SOLA VEZ)

git push origin master/main